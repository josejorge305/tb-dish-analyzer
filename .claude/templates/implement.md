@claude PLAN APPROVED

Implement the plan now.

Requirements:
- Keep diffs minimal (patch-only, no full rewrites)
- Add/update golden test(s)
- Open PR with:
  - **What** changed (1–2 sentences)
  - **Why** (rationale)
  - **How tested** (exact commands + results)
  - **Risks** (what could break, rollback steps)

Acceptance criteria:
- <List specific, verifiable outcomes>
- <e.g., "curl returns 200 with expected JSON schema">
- <e.g., "PIPELINE_VERSION unchanged unless approved">
